{
  "name": "fitbuddy",
  "version": "1.0.0",
  "description": "Application FitBuddy",
  "scripts": {
    "start": "cd backend && npm start",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false && cd backend && npm install && cd ../frontend && npm install && npm install -g react-scripts@5.0.1 && CI=false npm run build && cd ../backend"
  },
  "engines": {
    "node": "18.x",
    "npm": "10.x"
  }
}
